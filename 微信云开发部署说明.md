# 微信云开发静态托管部署说明

## 1. 准备工作

- 注册微信小程序账号（个人/企业均可）
- 进入 [微信云开发控制台](https://console.cloud.tencent.com/tcb) 创建一个云开发环境
- 记录环境 ID（在云开发控制台首页可以看到）

## 2. 安装 CloudBase CLI

```bash
npm i -g @cloudbase/cli
```

## 3. 登录授权

```bash
tcb login
```

执行后会打开浏览器，使用微信扫码授权。

## 4. 配置环境 ID

复制示例配置文件：

```bash
cp .env.example .env
```

编辑 `.env`，把 `your-env-id-here` 换成你的真实环境 ID：

```
TCB_ENV_ID=your-real-env-id
```

## 5. 开启静态网站托管

进入云开发控制台：

基础服务 -> 静态网站托管 -> 开启

## 6. 一键部署

```bash
bash deploy-cloudbase.sh
```

脚本会自动把当前目录下的 `index.html` 和 8 张包装图片上传到静态托管根目录。

## 7. 访问页面

部署完成后，在云开发控制台：

基础服务 -> 静态网站托管 -> 默认域名

复制默认域名即可在微信内打开。例如：

```
https://你的环境ID-xxx.tcloudbaseapp.com
```

## 8. 减少微信警告（可选）

如果要在微信内彻底避免“非微信官方网页”提示，可以在小程序或公众号后台的“业务域名”中添加该默认域名。
